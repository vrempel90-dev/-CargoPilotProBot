# Role based /start menu

Что изменено:

- админ при `/start` сразу видит админ-меню;
- склад при `/start` сразу видит складское меню;
- курьер при `/start` сразу видит курьерское меню;
- обычный клиент видит клиентское меню;
- команда `/admin`, `/warehouse`, `/courier` остаётся как резерв.

Для GitHub достаточно заменить:
- `main.py`
- `15_ROLE_BASED_START_MENU.md`
